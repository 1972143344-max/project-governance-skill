interface:
  display_name: "Project Governance"
  short_description: "Runtime-first governance, routing, sync, and structural upkeep."
  default_prompt: "Use $project-governance to route governance-sensitive work, read the right authority surfaces first, and handle runtime sync or structural maintenance without drifting into low-frequency initialization material unless this action truly needs it."
